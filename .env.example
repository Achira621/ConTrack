# Environment variables for ConTrack frontend

# Database connection (PostgreSQL)
# Get this from your database provider (Neon, Supabase, Vercel Postgres, etc.)
DATABASE_URL="postgresql://user:password@localhost:5432/contrack?schema=public"

# For serverless Postgres (e.g., Neon), use connection pooling
# DATABASE_URL="postgresql://user:password@host.neon.tech/contrack?sslmode=require"

# Node environment
NODE_ENV="development"

# Optional: Gemini API key (if using AI features)
GEMINI_API_KEY="your_api_key_here"
